# create a folder and go into that
# pip install virtualenv
# virtualenv virtualenv_name
# .\virtualenv_name\Scripts\activate
# (virtualenv_name)$ deactivate # if you want to deactivate that
# install modules and packages inside venv
# pip freeze > requirement.txt # picks all installed module with their required version inside the file

# When you want to use the same code inside another venv, 
# pip install -r requirement.txt